module Util where

-- CONTADOR DE PARENTESES
--parenthesesCounter :: program -> Int
    -- quantos parenteses abre



-- TUPLE PATTERN MATCHING
--fstElem :: (a, b, c) -> a
--fstElem = (e, _, _) = e

--sndElem :: (a, b, c) -> b
--sndElem = (_, e, _) = e

--trdElem :: (a, b, c) -> c
--trdElem = (_, _, e) = e